name: Auto-PR-Clickup

on:
  # push:
  #   branches:
  #     - 'test'
  pull_request:
    types: [opened, reopened]
  workflow_dispatch: # This event allows manual triggering

jobs:
  auto_sync_pr_clickup:
    runs-on: ubuntu-latest

    steps:
      - name: List Envs
        run: env

      - name: PR number
        run: |
          echo ${{ github.event.pull_request.number }}
          echo ${{ github.event.pull_request.repo }}

      - name: Pull Object
        run: |
          gh pr view ${{ github.event.pull_request.number }}

      # - name: Event Object
      #   run: |
      #     for element in ${{ github.event }}
      #       do
      #           echo "Current element: $element"
      #       done

      - name: Fill Clickup Pull Requests
        run: |
          echo "start here now"